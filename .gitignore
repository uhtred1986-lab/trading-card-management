# dependencies
/node_modules
/.pnp
.pnp.*

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# misc
.DS_Store
*.pem
*.tsbuildinfo

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files — never commit
.env
.env.*
!.env.example

# vercel
.vercel

# local data caches (catalog dumps, scan temp)
/.cache/
/tmp/

.vercel
.env*

# Official rule PDFs are downloaded into docs/rules by hand (see docs/arena-design-proposal.md); only the extracted text is tracked.
docs/rules/rulemanual.pdf
docs/rules/playmanual.pdf
docs/rules/unison_flowchart.pdf

# Kotlin/Gradle build output (android/ — see docs/arena-android-spec.md §10).
# The toolchain runs in Docker; nothing here is installed on the machine.
android/build/
android/*/build/
android/.gradle/
android/local.properties
